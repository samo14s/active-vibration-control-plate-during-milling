# تحقّق من النموذج المنقول من مقالة Du et al. (IJMS 274, 2024, 109257)

مراجعة دقيقة للنموذج الفيزيائي الموجود في `simulation/` مقابل المقالة:

> J. Du, X. Liu, H. Dai, X. Long, *Robust combined time delay control for milling chatter
> suppression of flexible workpieces*, International Journal of Mechanical Sciences 274 (2024) 109257.

كل رقم في هذا التقرير قابل لإعادة الإنتاج عبر السكربتات في `verification/`
(تُشغَّل من جذر المستودع: `python3 verification/0X_*.py`).

---

## 1. الخلاصة

النقل **صحيح في جوهره**: معادلات القوى (3)–(4) ومعادلة الحركة (13) وخطة التكامل الزمني
منقولة بأمانة ومُتحقَّق منها عدديًا. نموذج الصفيحة بعناصر Kirchhoff‑Q4 أدقّ من نموذج
المقالة نفسها مقابل قياسات الجدول 4 (خطأ RMS ‏1.64 % مقابل 2.24 %).

لكن هناك **عيبًا واحدًا خطيرًا** (غياب التشبّع المُعلَن ±150 V)، و**خطأ منهجيًا في دالة
التحقق** `check_model`، و**ادّعاءً غير قابل لإعادة الإنتاج** في `model_v2.py` تُبنى عليه
كل نتائج `run_demo.py`. كما أنّ **كسب المشغّل الكهرضغطي `H_Pe` غير معايَر أصلًا**، لأنّ
الشكل 12 في المقالة غير قابل للاستخدام كمرجع مطلق.

---

## 2. ما هو منقول بأمانة ومُتحقَّق منه ✅

| البند | المرجع | النتيجة |
|---|---|---|
| أبعاد الصفيحة ومادّتها (100×80×4 mm, AL6061, 2830 kg/m³, 69 GPa, 0.33) | Table 1 | مطابق تمامًا |
| أداة القطع (3 أسنان، Ø10 mm، η=35°، γn=15°، kt=925 MPa، kn=0.26، μc=0.2) | Table 3 | مطابق تمامًا |
| `k1 = kn/cos η = 0.317401`، `k2 = 1+μc·tan η·(cos γn − kn·sin γn) = 1.125846` | Eq. (3) | مطابق تمامًا |
| تركيبات `α3 = k2·kt·sc − k1·kt·cc`… | Eq. (3) | مطابق تمامًا |
| تكاملات المسار الحلزوني `ss, sc, cc` | Eq. (4) | **خطأ 0.000 %** مقابل تربيع عددي مباشر (`02_*`) |
| `(K + α4 DᵀD)q(t) − α4 DᵀD q(t−τ) = fτ α3 Dᵀ + H_Pe u` | Eq. (13) | منقولة حرفيًا |
| Newmark ضمني (γ=1/2, β=1/4) | — | تنفيذ صحيح؛ `dt = τ/82` يُسقِط التأخير على الشبكة تمامًا |
| قطع هابط `φ_st = π − arccos(1 − ae/R) = 2.9413 rad`، `φ_ex = π`، `ae = 0.1 mm` | §4.2 | مطابق |
| موضع الحسّاس: الزاوية العليا اليمنى؛ القطع على الحافة العليا | §4.1، §5 | مطابق |
| `laplace_n_patch` = ∬∇²N dA | Eq. (14) | مُتحقَّق منه بنظرية التباعد، خطأ نسبي ‎1e‑5‏ (`06_*`) |
| تقارب الشبكة | — | 36×30 مُتقارِبة (فرق < 0.01 % مقابل 60×50) |
| اقتطاع 5 أنماط | §3.1 | يحتفظ بـ **94.7 %** من الليونة الساكنة |
| حدّ الاستقرار بلا تحكّم | Fig. 13(b) ≈ 0.03–0.05 mm | تحليليًا 0.0467 mm (أدنى قيمة على كل السرعات)، وبـ Newmark عند 4900 rpm ‏0.0605 mm |

---

## 3. انحرافات مُعلَنة عن المقالة (مقبولة، بل بعضها تحسين)

**3.1 التقطيع المكاني.** المقالة تستعمل Chebyshev–Ritz مع مبدأ تغايري مُعدَّل
(الحدّان `Kλ` و`Kκ`)؛ الشيفرة تستعمل عناصر منتهية Kirchhoff‑Q4 بدوال Hermite.
النتيجة أدقّ من نموذج المقالة (`08_*`):

| | 1st | 2nd | 3rd | 4th | 5th | RMS |
|---|---|---|---|---|---|---|
| قياس (Table 4) | 540 | 1068 | 2787 | 3351 | 4122 | — |
| الشيفرة v1 + رقعة بنيوية | **540.0** | 1091.4 | 2741.1 | 3406.6 | 4195.3 | **1.64 %** |
| نظري المقالة (Chebyshev–Ritz) | 537 | 1101 | 2805 | 3423 | 4254 | 2.24 % |
| صفيحة عارية (بلا رقعة) | 521.1 | 1069.9 | 2732.9 | 3334.3 | 4145.4 | 1.83 % |

**3.2 نمذجة الرقعة بنيويًا.** غير موجودة في معادلتَي (9)–(10) بالمقالة. إضافتها ترفع
النمط الأول من 521.1 إلى **540.0 Hz**، أي مطابقة تامّة لقياس الجدول 4. تصحيح مُبرَّر.

**3.3 إعادة المعايرة الترددية** على قِمم الشكل 12(a) المُرقمنة
`[536.4, 1071.8, 2778.6, 3358.7, 4122.6]` بدل قيم الجدول 4 `[540, 1068, 2787, 3351, 4122]`
(فروق ‎0.2–0.7 %‎). أسلوب *model updating* مشروع، لكنه يكسر التوافق `Kp = VᵀK_freeV`.

**3.4 صيغة الاقتران الكهرضغطي — انحراف غير مُعلَن.** الشيفرة تستعمل نموذج العزم المكافئ
الكلاسيكي `m_piezo = −E_Pe·d31·(bP+hPa)/(2(1−νPe))` مضروبًا في اللابلاسيان **الفيزيائي**،
بينما المقالة تعطي `C_P0` عبر المعادلة (15). عدديًا المعاملان متقاربان:

```
Eq.(15) :  −C_P0·d31/hPa = −0.033387 N/V
الشيفرة  :  m_piezo      = −0.039860 N/V      (+19 %)
```

لكن **المعادلة (14) كما هي مكتوبة غير متجانسة بُعديًا**: تكاملاتها في إحداثيات لا‑بُعدية،
فتزن انحناء المحور x بمعامل `lp/hp = 1.25` وانحناء المحور z بمعامل `hp/lp = 0.80`، بينما
اللابلاسيان الفيزيائي متماثل المناحي. **اختيار الشيفرة هو الصحيح فيزيائيًا**، والعيب في
المقالة — لكن الانحراف غير موثّق في الشيفرة.

**3.5 قصور الدوران مُهمَل.** المعادلة (A.1) في المقالة تتضمّن حدّي `I2`؛ مصفوفة الكتلة في
الشيفرة هي `ρh∫NᵀN` فقط. الأثر ‎≈ −0.8 %‎ على النمط الخامس، تبتلعه المعايرة.

---

## 4. عيوب حقيقية يجب إصلاحها ⚠️

### F1 — التشبّع ±150 V **غير مُطبَّق** (الأخطر)

`simulation_base.py` (سطر 56) و`sim_kit/README.md` (سطر 36) يعلنان صراحةً:
> «Le simulateur sature lui‑meme la commande a +/- V_MAX»

**هذا غير صحيح.** في `newmark_solver.py:138` التشبّع يُطبَّق فقط إذا `piezo is not None`،
و`SimBase.run` لا يمرّر `piezo` أبدًا. تحقّق مباشر: مصحّح يُرجع 10 000 V ⇒ `peak_u = 10000.0 V`.

> أي مصحّح لا يشبّع داخليًا يحصل على سلطة غير محدودة، فتفقد كل المقارنات معناها.
> **الإصلاح:** قصّ `u_actual` على `±V_MAX` داخل `newmark_solver.simulate`.

### F2 — `check_model()` يقارن دالة استجابة خاطئة

الشكل 12(a) هو استجابة **نقطة القيادة**: المطرقة والحسّاس كلاهما في الزاوية العليا اليمنى
(§4.1). لكن `simulation_base.py:338` يحسب `G = |Σ (D_obs·D_tool)/den|`، أي استجابة **نقل**
بين موضع الأداة (x = 0) والحسّاس. القياس الصحيح هو `D_obs·D_obs` (`07_*`):

| الطريقة | مضادات الرنين (Hz) | الفروق مقابل [734, 2161, 3001, 3805] | RMS |
|---|---|---|---|
| `D_obs·D_tool` (الحالية) | 795, 2101, 3001, 3787 | +8.4 %, 2.8 %, 0.0 %, 0.5 % | 4.41 % |
| `D_obs·D_obs` (الصحيحة) | 731, 2118, 3008, 3943 | **+0.4 %**, 2.0 %, 0.2 %, 3.6 % | **2.08 %** |

الاختبار الحالي يقيس الكمّية الخطأ **ويبخس النموذج حقّه** في آن واحد.

### F3 — نسب التخميد للنمطين 4 و5 لا تطابق الجدول 4

القاعدة المجمَّدة تستعمل `0.30 % / 0.30 %`، والقياس المنشور `0.56 % / 0.35 %`.
`model_v2.py` يصحّح هذا وهو محقّ. الأثر على مراجع الحلقة المفتوحة معدوم
(`ap_lim` و`RMS` بلا تغيير حتى الرقم الرابع)، لكنه مهمّ لتسرّب التحكّم عند 3359 و4123 Hz.

### F4 — الادّعاء المركزي في `model_v2.py` لا يُعاد إنتاجه

يقول التوثيق إنّ وضع الرقعة أسفل‑يمين أفقيًا هو «الوضع الوحيد الذي يعيد إنتاج مضادات
الرنين الثلاثة المقيسة (787 / 1493 / 3609 Hz، نمط 4/4)». الحساب الفعلي (`07_*`):

```
||H|| = 0.5546              ← مطابق للتوثيق (إذن الإعداد مُعاد إنتاجه بدقّة)
مضادات الرنين D_obs·H = [817, 2654, 2987, 3754]
المُدّعى              = [787, 1493, 3609]
```

**مضادّ الرنين عند 1493 Hz غائب تمامًا** (النموذج يضع واحدًا عند 2654 Hz بدله).
أسوأ من ذلك، هندسة v2 ترفع النمط الأول إلى **575.4 Hz، أي +6.5 %** عن قياس الجدول 4
(540 Hz)، مقابل **540.0 Hz (0.0 %)** لهندسة v1. الأدلّة المتاحة تدعم إعدادَ v1
(أسفل‑يسار، 20 mm × 60 mm) لا v2 — و`run_demo.py` يبني كل نتائجه المعلَنة على v2.

### F5 — تربيع Gauss غير دقيق في `mass_matrix_K`

`kirchhoff_q4.py:174‑176` يستعمل عُقدًا وأوزانًا بثلاث منازل عشرية (مجموع الأوزان = 2.001).
النتيجة: **كتلة زائدة بمقدار +0.100 % بالضبط**، وترددات أقلّ بـ 0.05 %.
الإصلاح سطر واحد: `np.polynomial.legendre.leggauss(5)`.

### F6 — انظر 3.5 (قصور الدوران)

### F7 — مساحة الرقعة غير متّسقة بين الاقتران والصلابة

`_add_patch_structure` يختار العناصر بمركز ثقلها ⇒ الرقعة البنيوية **19.44 × 61.33 mm**
بدل 20 × 60 mm، بينما `laplace_n_patch` يتكامل على المستطيل الدقيق. الصلابة/الكتلة
المضافة والاقتران الكهرضغطي لا يعملان على المساحة نفسها.

### F8 — ادّعاء «النمط الخطأ» مبالَغ فيه

`README.md` يقول إنّ تردّد الرجفان 1135 Hz في المقالة هو «le mauvais mode». تحليليًا (`04_*`):

```
النمط 1 وحده ( 536.4 Hz) : ap_lim = 0.0494 mm
النمط 2 وحده (1071.8 Hz) : ap_lim = 0.0466 mm    ← فارق 6 % فقط
الأنماط 3–5              : ap_lim = 0.87–1.09 mm
```

النمطان **متعادلان عمليًا** في الحرجية، وأيّهما يفقد الاستقرار أوّلًا يقع داخل عدم يقين
النموذج. كما أنّ **تجربة المقالة نفسها** (Fig. 20) تُبلغ عن `fc1 = 580 Hz` **و**`fc2 = 1123 Hz`
كأكبر قمّتين معًا. الادّعاء غير مدعوم بهذه الصيغة القاطعة.

### F9 — المستوى المطلق لا يمكن التوفيق مع الشكل 12 — والخطأ في الشكل 12

| | ليونة ساكنة عند الزاوية العليا اليمنى |
|---|---|
| عناصر منتهية دقيقة (كل الدرجات) | **6.90 µm/N** |
| اقتطاع 5 أنماط | 6.53 µm/N (94.7 %) |
| حدّ عارضة مُنشَبة بعرض كامل | 4.64 µm/N |
| الشكل 12(a) المُرقمن (f < 140 Hz، 32.83 dB re 1 µm/N) | **43.8 µm/N ⇒ ×6.35** |

قيمة 43.8 µm/N غير قابلة للتحقّق فيزيائيًا لصفيحة الجدول 1. والأهمّ: **منحنيات الاستقرار
في الشكل 13 من المقالة نفسها (≈0.03–0.05 mm) متّسقة مع القيمة الأصلب** — صفيحة أليَن بـ 6.35
مرّة كانت ستعطي ‎≈0.008 mm‎. الاستنتاج: مرجع الديسيبل في الشكل 12 غير صحيح.

> **بالتالي لا يمكن معايرة `H_Pe` مقابل الشكل 12(b) أيضًا: مقدار كسب المشغّل في النموذج
> المنقول غير معايَر عمليًا** — وهو بالضبط الوسيط الذي يحدّد كل أرقام الأداء في الحلقة المغلقة.
> ما يمكن التحقّق منه هو *شكل* الاستجابة (مواضع مضادات الرنين) لا مستواها.

### F10 — تناقض داخلي في المقالة، مَنقول بأمانة

انطلاقًا من معادلات المقالة (1)، (2)، (5)، (10) و(A.4) — مع `f_y = −F_y` و
`y_r = −y_P(t) + y_P(t−τ)` — نحصل على:

```
M q̈ + C q̇ + (K − α4 DᵀD) q(t) + α4 DᵀD q(t−τ) = −fτ α3 Dᵀ
```

بينما المعادلة (12)/(13) في المقالة تعطي الإشارات **معكوسة بالكامل** (وهو ما ينتج عن
`f_y = +F_y`). الشيفرة تنسخ المعادلة (13). وبما أنّ متوسط `α4 < 0` (‎−3.76e7 N/m لكل متر
من `ap`)، فإنّ المعادلة (13) تجعل عملية القطع **صلابةً سالبة**، فيقع الرجفان **تحت** الرنين:

| | ap_lim | تردد الرجفان |
|---|---|---|
| المعادلة (12) كما كُتبت (= الشيفرة) | 0.0467 mm | 1070.0 Hz (< f₂ = 1071.8) — ومحاكاةً 524.6 Hz (< f₁ = 536.4) |
| الإشارة المشتقّة من (1)(2)(5)(10)(A.4) | 0.0467 mm | 1073.6 Hz (> f₂) |

الترددات المقيسة في المقالة تقع **فوق** الترددات الذاتية بـ 5–7 %.
**تحفّظ:** إزالة المادة رفعت f₁ بنسبة 17 % خلال حملة التجارب، لذا لا تحسم القياسات
هذه الإشارة. أثرها على حدّ الاستقرار **معدوم**، لكنها تمسّ كل ما يعتمد على تردد الرجفان.

### F11 — ملاحظات هندسة برمجية

* `model_v2.py:16` مسار مُصلَّب `/home/claude/work/sim_kit` (غير موجود).
* `run_demo.py` يعدّل `SB.PATCH` و`SB.ZETA_MODES` على مستوى الوحدة (أثر جانبي عام دائم).
* `newmark_solver.py:111‑124` يوزّع الاستدعاءات حسب `controller.__class__.__name__` مقابل
  قائمة أسماء مُصلَّبة — هشّ جدًا؛ يُفضَّل `hasattr` أو توقيع موحّد.
* `run_demo.blim` قد يخرج من حلقة التوسيع و`ok(hi)` ما يزال `True`، ثم يُنصِّف بافتراض العكس.
* **ثلاثة آفاق زمنية مختلفة**: `run`/`multi_speed_cost` بـ `T=0.20`، `stability_limit` بـ `T=0.28`،
  `run_demo.blim` بـ `T=0.45` — و`README` نفسه ينبّه أنّ `T` يُزيح الحدّ المقيس بنحو 20 %.
  إذن المرجعان «0.0605 mm» و«‎~0.049 mm‎» **غير قابلين للمقارنة**.

---

## 5. ترتيب أولويات الإصلاح

| # | البند | الأثر |
|---|---|---|
| 1 | F1 — تطبيق التشبّع ±150 V في المُحاكي | يُبطِل صحّة كل المقارنات بين المصحّحات |
| 2 | F4 — إعادة النظر في هندسة الرقعة في `model_v2` | كل أرقام `run_demo.py` مبنية عليها |
| 3 | F2 — استعمال `D_obs·D_obs` في `check_model` | يُصحّح التحقّق ويرفع تقدير النموذج |
| 4 | F9 — توثيق أنّ `H_Pe` غير معايَر | يمنع استنتاجات أداء مطلقة غير مؤسَّسة |
| 5 | F11 — توحيد الأفق `T` في كل المقاييس | يجعل المراجع قابلة للمقارنة |
| 6 | F3، F5، F7، F6 | تصحيحات عددية صغيرة، سهلة |
| 7 | F8، F10 | تصحيح صياغة الادّعاءات في التوثيق |

---

## 6. السكربتات

| السكربت | ما يتحقّق منه |
|---|---|
| `01_frf_vs_measured.py` | مضادات الرنين والمستويات، v1 مقابل v2، مقابل الشكل 12 |
| `02_static_gain_and_cutting_coeffs.py` | الكسب الساكن؛ تحقّق المعادلتين (3)–(4) بتربيع مباشر |
| `03_static_compliance_and_chatter.py` | الليونة الساكنة الدقيقة؛ الطيف |
| `04_stability_lobes.py` | فصوص الاستقرار التحليلية، مساهمة كل نمط |
| `05_alpha4_sign_convention.py` | تحقيق إشارة `α4` (F10) |
| `06_numerics_quadrature_mesh.py` | تربيع Gauss، نظرية التباعد، تقارب الشبكة، قصور الدوران |
| `07_model_v2_claims.py` | ادّعاءات `model_v2`؛ اختيار دالة الاستجابة في `check_model` |
| `08_modes_damping_patch_geometry.py` | عدد الأنماط، التخميد، هندسة الرقعة |

المتطلّبات: `numpy`, `scipy`, `openpyxl`.
